{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "go build -gcflags=all=\"-N -l\" ${workspaceFolder}/${relativeFile}",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}